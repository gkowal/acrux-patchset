diff '--color=auto' -uNr linux-6.12.1/arch/sparc/mm/init_64.c linux-6.12.1-p1/arch/sparc/mm/init_64.c
--- linux-6.12.1/arch/sparc/mm/init_64.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/arch/sparc/mm/init_64.c	2024-11-22 21:27:21.906696339 -0300
@@ -3073,11 +3073,11 @@
 static void __init kernel_lds_init(void)
 {
 	code_resource.start = compute_kern_paddr(_text);
-	code_resource.end   = compute_kern_paddr(_etext - 1);
+	code_resource.end   = compute_kern_paddr(_etext) - 1;
 	data_resource.start = compute_kern_paddr(_etext);
-	data_resource.end   = compute_kern_paddr(_edata - 1);
+	data_resource.end   = compute_kern_paddr(_edata) - 1;
 	bss_resource.start  = compute_kern_paddr(__bss_start);
-	bss_resource.end    = compute_kern_paddr(_end - 1);
+	bss_resource.end    = compute_kern_paddr(_end) - 1;
 }
 
 static int __init report_memory(void)
diff '--color=auto' -uNr linux-6.12.1/drivers/base/firmware_loader/main.c linux-6.12.1-p1/drivers/base/firmware_loader/main.c
--- linux-6.12.1/drivers/base/firmware_loader/main.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/drivers/base/firmware_loader/main.c	2024-11-22 21:27:21.914696279 -0300
@@ -899,6 +899,11 @@
 
 	ret = _request_firmware_prepare(&fw, name, device, buf, size,
 					offset, opt_flags);
+
+#ifdef CONFIG_GENTOO_PRINT_FIRMWARE_INFO
+        printk(KERN_NOTICE "Loading firmware: %s\n", name);
+#endif
+
 	if (ret <= 0) /* error or already assigned */
 		goto out;
 
diff '--color=auto' -uNr linux-6.12.1/fs/namei.c linux-6.12.1-p1/fs/namei.c
--- linux-6.12.1/fs/namei.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/fs/namei.c	2024-11-22 21:27:21.904696354 -0300
@@ -1034,10 +1034,10 @@
 		path_put(&last->link);
 }
 
-static int sysctl_protected_symlinks __read_mostly;
-static int sysctl_protected_hardlinks __read_mostly;
-static int sysctl_protected_fifos __read_mostly;
-static int sysctl_protected_regular __read_mostly;
+static int sysctl_protected_symlinks __read_mostly = 1;
+static int sysctl_protected_hardlinks __read_mostly = 1;
+int sysctl_protected_fifos __read_mostly = 1;
+int sysctl_protected_regular __read_mostly = 1;
 
 #ifdef CONFIG_SYSCTL
 static struct ctl_table namei_sysctls[] = {
diff '--color=auto' -uNr linux-6.12.1/kernel/sys.c linux-6.12.1-p1/kernel/sys.c
--- linux-6.12.1/kernel/sys.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/kernel/sys.c	2024-11-22 21:27:21.907696332 -0300
@@ -2732,10 +2732,16 @@
 		break;
 #endif
 	case PR_SET_MDWE:
-		error = prctl_set_mdwe(arg2, arg3, arg4, arg5);
+		if (IS_ENABLED(CONFIG_PARISC))
+			error = -EINVAL;
+		else
+			error = prctl_set_mdwe(arg2, arg3, arg4, arg5);
 		break;
 	case PR_GET_MDWE:
-		error = prctl_get_mdwe(arg2, arg3, arg4, arg5);
+		if (IS_ENABLED(CONFIG_PARISC))
+			error = -EINVAL;
+		else
+			error = prctl_get_mdwe(arg2, arg3, arg4, arg5);
 		break;
 	case PR_PPC_GET_DEXCR:
 		if (arg3 || arg4 || arg5)
diff '--color=auto' -uNr linux-6.12.1/kernel/trace/bpf_trace.c linux-6.12.1-p1/kernel/trace/bpf_trace.c
--- linux-6.12.1/kernel/trace/bpf_trace.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/kernel/trace/bpf_trace.c	2024-11-22 21:27:21.911696302 -0300
@@ -1025,7 +1025,7 @@
 };
 
 #ifdef CONFIG_X86_KERNEL_IBT
-static unsigned long get_entry_ip(unsigned long fentry_ip)
+static unsigned long __maybe_unused get_entry_ip(unsigned long fentry_ip)
 {
 	u32 instr;
 
diff '--color=auto' -uNr linux-6.12.1/net/bluetooth/hci_conn.c linux-6.12.1-p1/net/bluetooth/hci_conn.c
--- linux-6.12.1/net/bluetooth/hci_conn.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/net/bluetooth/hci_conn.c	2024-11-22 21:27:21.908696324 -0300
@@ -2369,8 +2369,13 @@
 		return 0;
 	}
 
-	if (hci_conn_ssp_enabled(conn) &&
-	    !test_bit(HCI_CONN_ENCRYPT, &conn->flags))
+	/* If Secure Simple Pairing is not enabled, then legacy connection
+	 * setup is used and no encryption or key sizes can be enforced.
+	 */
+	if (!hci_conn_ssp_enabled(conn))
+		return 1;
+
+	if (!test_bit(HCI_CONN_ENCRYPT, &conn->flags))
 		return 0;
 
 	return 1;
diff '--color=auto' -uNr linux-6.12.1/scripts/sign-file.c linux-6.12.1-p1/scripts/sign-file.c
--- linux-6.12.1/scripts/sign-file.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/scripts/sign-file.c	2024-11-22 21:27:21.912696294 -0300
@@ -51,9 +51,10 @@
  * signing with anything other than SHA1 - so we're stuck with that if such is
  * the case.
  */
-#if defined(LIBRESSL_VERSION_NUMBER) || \
-	OPENSSL_VERSION_NUMBER < 0x10000000L || \
-	defined(OPENSSL_NO_CMS)
+#if defined(OPENSSL_NO_CMS) || \
+	( defined(LIBRESSL_VERSION_NUMBER) \
+	&& (LIBRESSL_VERSION_NUMBER < 0x3010000fL) ) || \
+	OPENSSL_VERSION_NUMBER < 0x10000000L
 #define USE_PKCS7
 #endif
 #ifndef USE_PKCS7
diff '--color=auto' -uNr linux-6.12.1/tools/lib/bpf/elf.c linux-6.12.1-p1/tools/lib/bpf/elf.c
--- linux-6.12.1/tools/lib/bpf/elf.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/tools/lib/bpf/elf.c	2024-11-22 21:27:21.913696287 -0300
@@ -372,6 +372,11 @@
 	return ret;
 }
 
+#if !defined(__clang__)
+#pragma GCC diagnostic push
+/* https://gcc.gnu.org/PR114952 */
+#pragma GCC diagnostic ignored "-Wmaybe-uninitialized"
+#endif
 /* Find offset of function name in ELF object specified by path. "name" matches
  * symbol name or name@@LIB for library functions.
  */
@@ -387,6 +392,9 @@
 	elf_close(&elf_fd);
 	return ret;
 }
+#if !defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
 
 struct symbol {
 	const char *name;
diff '--color=auto' -uNr linux-6.12.1/tools/lib/subcmd/parse-options.c linux-6.12.1-p1/tools/lib/subcmd/parse-options.c
--- linux-6.12.1/tools/lib/subcmd/parse-options.c	2024-11-22 11:30:26.000000000 -0300
+++ linux-6.12.1-p1/tools/lib/subcmd/parse-options.c	2024-11-22 21:27:21.910696309 -0300
@@ -807,7 +807,7 @@
 static struct option *options__order(const struct option *opts)
 {
 	int nr_opts = 0, nr_group = 0, nr_parent = 0, len;
-	const struct option *o, *p = opts;
+	const struct option *o = opts, *p;
 	struct option *opt, *ordered = NULL, *group;
 
 	/* flatten the options that have parents */

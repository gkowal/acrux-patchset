diff -uNr linux-4.18.1-r4/drivers/pci/rom.c linux-4.18.1-r5/drivers/pci/rom.c
--- linux-4.18.1-r4/drivers/pci/rom.c	2018-08-15 12:37:34.000000000 -0300
+++ linux-4.18.1-r5/drivers/pci/rom.c	2018-08-16 06:21:21.718885915 -0300
@@ -91,8 +91,11 @@
 		void __iomem *pds;
 		/* Standard PCI ROMs start out with these bytes 55 AA */
 		if (readw(image) != 0xAA55) {
-			pci_info(pdev, "Invalid PCI ROM header signature: expecting 0xaa55, got %#06x\n",
-				 readw(image));
+			/* Some AMD PCI ROMs start out with bytes FF FF */
+			if (readw(image) != 0xFFFF) {
+				pci_info(pdev, "Invalid PCI ROM header signature: expecting 0xaa55, got %#06x\n",
+					 readw(image));
+			}
 			break;
 		}
 		/* get the PCI data structure and check its "PCIR" signature */

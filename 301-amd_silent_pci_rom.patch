diff -uNr linux-4.16.2-r4/drivers/pci/rom.c linux-4.16.2-r5/drivers/pci/rom.c
--- linux-4.16.2-r4/drivers/pci/rom.c	2018-04-12 07:30:01.000000000 -0300
+++ linux-4.16.2-r5/drivers/pci/rom.c	2018-04-17 13:21:52.348711619 -0300
@@ -93,8 +93,11 @@
 		void __iomem *pds;
 		/* Standard PCI ROMs start out with these bytes 55 AA */
 		if (readw(image) != 0xAA55) {
-			pci_info(pdev, "Invalid PCI ROM header signature: expecting 0xaa55, got %#06x\n",
-				 readw(image));
+			/* Some AMD PCI ROMs start out with bytes FF FF */
+			if (readw(image) != 0xFFFF) {
+				pci_info(pdev, "Invalid PCI ROM header signature: expecting 0xaa55, got %#06x\n",
+					 readw(image));
+			}
 			break;
 		}
 		/* get the PCI data structure and check its "PCIR" signature */
